-- 000002 · 回滚：无操作（刻意）
--
-- 被删除的 sys_dict_dict_type_key 是一个已确认的缺陷约束（S-1）。
-- 若在 down 中重建它，会：
--   1. 把缺陷重新引入；
--   2. 在从未有过该约束的新库（由 000001 建）上凭空创建一个错误的约束。
--
-- 因此 down 保持空操作，这是有意的单向修复迁移。
-- make migrate-verify 的 up → down → up 仍能通过（两次 up 都是幂等空操作或修复）。

SELECT 1;
